#behat.yml
default:
    suites:
        Family:
            contexts:
                - FamilyContext
            paths:    [ %paths.base%/features/family ]
        Sex:
            contexts:
                - SexContext
            paths:    [ %paths.base%/features/sex ]
        Line:
            contexts:
                - LineContext
            paths:    [ %paths.base%/features/line ]
        Model:
            contexts:
                - ModelContext
            paths:    [ %paths.base%/features/model ]

